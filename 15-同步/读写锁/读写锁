读写锁即针对读写操作的互斥锁，他与普通的互斥锁最大的不同，就是可以分别针对读操作和写操作进行锁定和解锁操作，读写锁遵循的控制访问规则与互斥锁有所不同
读写锁控制的多个写操作之间是都是互斥的，并且写操作之间也都是互斥的。但是多个读操作之间却不存在互斥关系。这样的互斥策略之下，读写锁可以大大降低因为使用
锁造成的性能损耗的情况下，完成对共享资源的访问控制

sync.RWMutex


//写
func (*RWMutex) Lock()
func (*RWMutex) Unlock()

//读
func (*RWMutex) RLock()
func (*RWMutex) RUnlock()

